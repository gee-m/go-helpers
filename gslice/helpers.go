package gslice

/* Add this to user snippets for go
   "Remove element of index i from slive": {
       "prefix": "gslice.Remove",
       "body": [
           "${1:array} = append(${1:array}[:${2:i}], ${1:array}[${2:i}+1:]...)"
       ]
   },
*/

// TODO CREATE A BOOLEAN LOGIC "RULES" PACKAGE, WHERE EASY DEFINE FUNCTION FOR
// MAKING LOGIC MORE READABLE!!!
